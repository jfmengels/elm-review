{
  "name": "",
  "version": "1.0.0",
  "scripts": {
    "test": "elm make --docs=/tmp/docs.json && elm-format src/ --validate && elm-test && npm run phantom-tests && elm-review",
    "phantom-tests": "node phantom-type-tests/run.js"
  },
  "dependencies": {
    "elm": "^0.19.1-3",
    "elm-format": "^0.8.3",
    "elm-review": "^2.0.0-beta.1",
    "elm-test": "^0.19.1-revision2"
  }
}
